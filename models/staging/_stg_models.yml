version: 2

models:
    - name: stg_users
      description: "Users from the app"
      columns:
          - name: id
            description: "The primary key for the user"
            tests:
                - unique
                - not_null
